<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>To-Do List</title>
    <style>
        * { margin: 0; padding: 0; box-sizing: border-box; font-family: 'Arial', sans-serif; }
        body { background: linear-gradient(to bottom, #1E88E5, #A1C4FD); display: flex; justify-content: center; align-items: center; height: 100vh; }
        .container { width: 50%; background: #fff; padding: 20px; border-radius: 10px; box-shadow: 0px 4px 10px rgba(0, 0, 0, 0.1); text-align: center; }
        h1 { color: #333; margin-bottom: 20px; }
        .task-form { display: flex; justify-content: center; margin-bottom: 20px; }
        input { flex: 1; padding: 10px; border: none; outline: none; border-radius: 5px; width: 60%; }
        .add-btn { background: #FF5722; color: white; border: none; padding: 10px; cursor: pointer; border-radius: 5px; margin-left: 10px; }
    </style>
</head>
<body>
    <div class="container">
        <h1>To-Do List</h1>
        <form action="/add" method="post" class="task-form">
            <label>Enter Your Task</label>
            <input type="text" name="task" placeholder="Add a new task..." required>
            <label>Enter Your Date</label>
            <input type="date" name="task" placeholder="Add Date..." required>
            <button type="submit" class="add-btn">+</button>
        </form>
    </div>
</body>
</html>
