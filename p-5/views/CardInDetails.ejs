<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title><%= movie.movieTitle %> - Movie Details</title>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            background: linear-gradient(135deg, #1f1f1f, #000000);
            color: #f8f9fa;
        }
        .card-custom {
            background: #1e1e2f;
            border: 1px solid #444;
        }
        .info-label {
            font-weight: 600;
            color: #adb5bd;
        }
        .info-value {
            color: #f8f9fa;
        }
        .text-shadow {
            text-shadow: 1px 1px 2px black;
        }
        .movie-title {
            font-size: 2.2rem;
            font-weight: bold;
            color: #ff5252;
        }
        .badge-genre {
            background: #17a2b8;
            font-size: 0.9rem;
        }
    </style>
</head>
<body>
    <div class="container py-5">
        <div class="card card-custom shadow-lg rounded-4 overflow-hidden">
            <div class="row g-0">
                <!-- Movie Poster -->
                <div class="col-md-4 p-4 text-center bg-black">
                    <img src="<%= movie.avatar %>" alt="Movie Poster" class="img-fluid rounded-3 shadow-lg" style="max-height: 340px;">
                </div>

                <!-- Movie Info -->
                <div class="col-md-8">
                    <div class="card-body p-4">
                        <h1 class="movie-title mb-3 text-shadow"><%= movie.movieTitle %></h1>
                        <div class="mb-3">
                            <span class="info-label">🎬 Director:</span>
                            <span class="info-value text-warning"> <%= movie.directorName %></span>
                        </div>
                        <div class="mb-3">
                            <span class="info-label">📅 Release Year:</span>
                            <span class="info-value text-info"> <%= movie.releaseYear %></span>
                        </div>
                        <div class="mb-3">
                            <span class="info-label">🎭 Genre:</span>
                            <span class="badge badge-genre text-white"><%= movie.Genre %></span>
                        </div>
                        <div class="mb-3">
                            <span class="info-label">📜 Description:</span>
                            <p class="info-value mt-1"><%= movie.description %></p>
                        </div>
                        <a href="/" class="btn btn-outline-light mt-4">⬅ Back to Movies</a>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
