<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Movie Details</title>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
    <div class="container py-5">
        <div class="card bg-dark text-white shadow-lg rounded-4">
            <div class="row g-0">
                <!-- Movie Poster -->
                <div class="col-md-4 text-center p-4">
                    <img src="<%= movie.avatar %>" alt="Movie Poster" class="img-fluid rounded-3 shadow" style="max-height: 300px;">
                </div>
                
                <!-- Movie Details -->
                <div class="col-md-8">
                    <div class="card-body p-4">
                        <!-- Movie Title -->
                        <h2 class="card-title text-danger mb-4 text-shadow fw-bold"><%= movie.movieTitle %></h2>
                        <ul class="list-unstyled fs-5">
                            <!-- Director -->
                            <li class="mb-2">
                                🎬 <strong>Director:</strong> <span class="text-warning"><%= movie.directorName %></span>
                            </li>
                            <!-- Release Year -->
                            <li class="mb-2">
                                📅 <strong>Release Year:</strong> <span class="text-info"><%= movie.releaseYear %></span>
                            </li>
                            <!-- Genre -->
                            <li class="mb-2">
                                🎭 <strong>Genre:</strong> <span class="text-success"><%= movie.Genre %></span>
                            </li>
                            <!-- Description -->
                            <li class="mb-2">
                                📜 <strong>Description:</strong> <span class="text-warning"><%= movie.description %></span>
                            </li>
                        </ul>
                        <a href="/" class="btn btn-outline-light mt-4">⬅ Back to Movies</a>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
